# GenerateCSExportStats
Powershell script that will generate CSExport reports and capture statistics from each AAD Connect management agent (Connector) to XML and ZIP up the resulting files for submission.
